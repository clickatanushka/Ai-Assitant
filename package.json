{
  "name": "audit-assistant-blob",
  "private": true,
  "description": "Only dependency is the Vercel Blob client, used by api/blob.ts for >4.5 MB uploads. The rest of the backend is Python.",
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  }
}
